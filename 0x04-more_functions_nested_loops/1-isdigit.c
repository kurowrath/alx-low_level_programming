#include "main.h"

/**
 * _isdigit - check id c is digit
 *
 * @c: input
 *
 * Return: 1 if its digit, 0 if not
*/

int _isupper(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
