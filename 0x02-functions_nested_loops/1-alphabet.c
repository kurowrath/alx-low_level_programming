#include "main.h"
/**
 * main - Entry point 
 *
 * Description :  printing alphabets
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	char a;
	
	for (char a = 'a' ; a <= 'z' ; a++)
	{
		_putchar(a);
	}
return(0);
}
