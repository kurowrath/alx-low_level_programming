#include "main.h"

/**
 * int _strlen - returns the length of a string.
 *
 * @s: input parameter
 *
 * Return: Nothing
 */

int _strlen(char *s)
{
	int counter;

	for (counter =0; *s != '\0'; s++)
		++counter;

	return (countr);
}

